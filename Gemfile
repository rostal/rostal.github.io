# frozen_string_literal: true

source "https://rubygems.org"

gem 'jekyll-scholar', group: :jekyll_plugins
gem 'rake', '>= 12.0'      # override gemspec ~>12.0 to allow rake 13.x
gem 'bigdecimal'            # removed from Ruby default gems in 3.4+; required by liquid

gemspec

